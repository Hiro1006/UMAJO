<!DOCTYPE html>
<html>
  <head>
    <title>UMAJO</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div class="header">
      <div class="header-logo">UMA<span>J</span>O</div>
      <div class="header-list">
        <ul>
          <% if user_signed_in? %>
            <li><%= link_to "ログアウト", destroy_user_session_path, class: "shadow", method: :delete %></li>
          <% else %>
            <li><%= link_to "新規登録", new_user_registration_path, class: "shadow" %></li>
            <li><%= link_to "ログイン", new_user_session_path, class: "shadow" %></li>
          <% end %>
        </ul>
      </div>
    </div>
    <%= yield %>
  </body>
</html>
